// Remove the 'import type' line completely

/** @type {import('next').NextConfig} */
const nextConfig = {
  /* your config options here */
  reactStrictMode: true,
  trailingSlash: false,
};

export default nextConfig;